"""«Таблицы» — свои таблицы и полка, на которой они хранятся (удаляемый модуль).

Сервер здесь только хранит: снимок книги Univer лежит на полке строкой и
отдаётся обратно как есть. Импорт из Google и из файла `.xlsx` целиком живёт в
браузере (`src/components/web-excel/`): сервер книги не разбирает, в Google не
ходит и чужих кредов не держит.

Раньше раздел был зеркалом книг BBC, читал их сервисным аккаунтом дашборда и
был закрыт входом дашборда. Это и было главным потребителем памяти процесса:
сырой ответ Google с оформлением для одной вкладки весил сотни мегабайт.

Модуль самодостаточен: свои настройки и своя схема в БД. Удалить папку и одну
строку в `app/api/router.py` — и раздел исчезает, не задев ничего другого.
"""
