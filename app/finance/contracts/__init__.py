"""Реестр договоров — верхнее звено учёта «Финансов».

Договор вводится один раз; листы реестра — это отборы, а не копии; таблица и
карточка правят одну и ту же запись. Почему так и что из этого следует для
модели — в `models.py`. План работ — `~/.claude/plans/…axolotl.md`.

Пакет живёт внутри `app.finance` и подчиняется тем же правилам изоляции:
`app.bbc` не импортируется (проверяет `tests/test_finance_isolated.py`).
"""
