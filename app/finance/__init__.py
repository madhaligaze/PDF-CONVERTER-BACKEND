"""Раздел «Финансы» — управленческий учёт внутри приложения (удаляемый модуль).

Состав:
    `db.py`        — схема `finance`, сессии;
    `models.py`    — что считается правдой, а что производным;
    `service.py`   — справочники, операции, применение импорта;
    `importing.py` — чтение файлов: колонки по названиям, построчные замечания;
    `reports.py`   — деньги, прибыль, долги, проекты, календарь, план/факт;
    `grid.py`      — журнал как лист Univer и правка ячейки как правка операции.

Маршруты лежат снаружи пакета — `app/api/routes/finance.py`. Пакет ничего не
знает про учётки этого продукта, и это свойство проверяется тестом
`test_finance_isolated`.
"""
